import { SortViewsPipe } from './sort-views.pipe';

describe('SortViewsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortViewsPipe();
    expect(pipe).toBeTruthy();
  });
});
